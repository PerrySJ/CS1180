//Learned how to import the scanner based on Lab Instructor Blakes example.
import java.util.Scanner;
public class HappyBirthday 
{
    public static void main(String[] args) 
    {
    
      int day;
      int year;
      String month;

      System.out.print("When were you born? (Please use 1 January 2000 format)");

      // Learned how to actually set up the scanner from https://www.w3schools.com/java/java_user_input.asp
      Scanner scnr = new Scanner(System.in);
      
      //This is pulling the first integer typed in the scanner
      day = scnr.nextInt();
      //This is pulling the first string typed in the scanner
      month = scnr.next();
      //This is pulling the next integer after the first one, seperated by a space, typed in the scanner
      year = scnr.nextInt();

      System.out.println("Day: " + day);
      System.out.println("Month: " + month);
      System.out.println("Year: " + year);
      
      //Learned how to close the scanner from https://www.baeldung.com/java-scanner-close
      scnr.close();


    
    }
}
